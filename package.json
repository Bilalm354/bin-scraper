{
  "name": "bin-scraper",
  "version": "1.0.0",
  "description": "Gets bin information and emails users",
  "main": "main.js",
  "scripts": {
    "main": "cd next-app && npm run main",
    "test": "npx jest",
    "db-push": "npx drizzle-kit push",
    "db-cli": "export $(grep -v '^#' .env | xargs) && psql $DATABASE_URL",
    "next": "cd next-app && npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.10.7",
    "dotenv": "^16.4.7",
    "drizzle-kit": "^0.30.4",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  },
  "dependencies": {
    "drizzle-orm": "^0.39.3",
    "postgres": "^3.4.5",
    "zod": "^3.24.1"
  }
}
